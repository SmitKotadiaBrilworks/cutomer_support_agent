import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "UrbanCart Support — Multi-Agent POC",
  description: "LangGraph multi-agent customer support demo (Triage + Order/Returns + FAQ + Escalation agents)",
};

// Light theme only, deliberately — native form controls (the customer
// picker, scrollbars) should follow suit rather than switching dark under an
// OS dark-mode setting while the rest of the page stays light.
export const viewport: Viewport = {
  colorScheme: "light",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
